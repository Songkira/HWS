import sys; sys.stdin = open("4839.txt", "r")

T = int(input())

for tc in range(T):
    P, A, B = map(int, input().split())
    start = 1
    end = P

    cnt_A = 0
    cnt_B = 0

    while  == A:
        middle = (start + end)//2
        if middle == A:

        elif middle > A:
            end = middle -
