# import sys; sys.stdin = open("4836.txt", "r")

# 사각영역의 표현
N = 10
arr = [[0] * N for _ in range(N)]

# 1) 두 좌표로 사각영역을 표현
#   - 좌상단 꼭지점, 우하단 꼭지점
# 사각 영역의 순회
for r in range(r1, r2 +1):
    for c in range(c1, c2 + 1):
        arr[r][c]

# 2) 좌상단 꼭지점(r1,c1)과 너비(w)와 높이(h)
for r in range(r1, r2 + h):
    for c in range(c1, c2 + w):
        arr[r][c]